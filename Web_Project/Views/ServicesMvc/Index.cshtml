@* @model List<Web_Project.Models.ServiceDTO>

@{
    ViewData["Title"] = "Our Services";
}

<div class="space-y-6">
    <div class="text-center">
        <h1 class="text-3xl font-bold tracking-tight text-slate-900">Our Services</h1>
        <p class="mt-2 text-lg text-slate-600">Discover what we offer to help you reach your goals.</p>
    </div>

    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        @if (Model != null && Model.Any())
        {
            @foreach (var service in Model)
            {
                        <div class="flex flex-col overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200 transition hover:shadow-md">
                            <div class="p-6">
                                <div class="flex items-center justify-between">
                                    <h3 class="text-xl font-semibold text-slate-900">@service.ServiceName</h3>
                                    <span class="inline-flex items-center rounded-full bg-emerald-50 px-2.5 py-0.5 text-xs font-medium text-emerald-700 ring-1 ring-inset ring-emerald-600/20">
                                        $@service.FeesPerHour / hr
                                    </span>
                                </div>
                                <p class="mt-4 text-base leading-relaxed text-slate-600">
                            @service.Details
                                </p>
                            </div>

                            <div class="mt-auto border-t border-slate-100 bg-slate-50 px-6 py-4">
                        @if (User.IsInRole("Member"))
                        {
                                        <a asp-controller="AppointmentsMvc" asp-action="Book" asp-route-serviceId="@service.ServiceID"
                                           class="text-sm font-semibold text-emerald-600 hover:text-emerald-500">
                                            Book this service <span aria-hidden="true">&rarr;</span>
                                        </a>
                        }
                        else if (!User.Identity.IsAuthenticated)
                        {
                                        <a asp-area="Identity" asp-page="/Account/Login"
                                           class="text-sm font-semibold text-emerald-600 hover:text-emerald-500">
                                            Log in to book <span aria-hidden="true">&rarr;</span>
                                        </a>
                        }
                            </div>
                        </div>
            }
        }
        else
        {
                <div class="col-span-full text-center py-12 text-slate-500">
                    <p>No services are currently available. Please check back later!</p>
                </div>
        }
    </div>
</div> *@

                                            @model List<Web_Project.Models.ServiceDTO>

@{
    ViewData["Title"] = "Our Services";
}

<div class="mx-auto max-w-6xl px-4 py-8">
    <!-- Header -->
    <div class="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">
        <div>
            <h1 class="text-2xl font-semibold tracking-tight text-slate-900">Our Services</h1>
            <p class="mt-1 text-sm text-slate-600">Explore activities, pricing, and book your next session.</p>
        </div>

        <div class="flex flex-wrap gap-2">
            @if (User?.Identity?.IsAuthenticated ?? false)
            {
                @if (User.IsInRole("Member"))
                {
                            <a asp-controller="AppointmentsMvc" asp-action="Book"
                               class="inline-flex items-center justify-center rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-emerald-500">
                                Book Now
                            </a>

                            <a asp-controller="AppointmentsMvc" asp-action="MyAppointments"
                               class="inline-flex items-center justify-center rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-900 shadow-sm hover:bg-slate-50">
                                My Appointments
                            </a>
                }
            }
            else
            {
                    <a asp-area="Identity" asp-page="/Account/Login"
                       class="inline-flex items-center justify-center rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-900 shadow-sm hover:bg-slate-50">
                        Log in
                    </a>
            }
        </div>
    </div>

    <!-- Alerts (optional) -->
    @if (TempData["Success"] != null)
    {
            <div class="mt-4 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">
            @TempData["Success"]
            </div>
    }
    @if (TempData["Error"] != null)
    {
            <div class="mt-4 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">
            @TempData["Error"]
            </div>
    }

    <!-- Content -->
    <div class="mt-6">
        @if (Model != null && Model.Any())
        {
                <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
                @foreach (var service in Model)
                {
                            <div class="group flex flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-md">
                                <!-- Card Header -->
                                <div class="border-b border-slate-100 bg-slate-50 px-6 py-4">
                                    <div class="flex items-start justify-between gap-3">
                                        <div class="min-w-0">
                                            <h3 class="truncate text-lg font-semibold tracking-tight text-slate-900">
                                        @service.ServiceName
                                            </h3>
                                            <p class="mt-1 text-xs text-slate-500">
                                                Service ID: @service.ServiceID
                                            </p>
                                        </div>

                                        <span class="shrink-0 inline-flex items-center rounded-full border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">
                                            $@service.FeesPerHour / hr
                                        </span>
                                    </div>
                                </div>

                                <!-- Card Body -->
                                <div class="flex-1 px-6 py-5">
                            @if (!string.IsNullOrWhiteSpace(service.Details))
                            {
                                            <p class="text-sm leading-6 text-slate-600 line-clamp-4">
                                    @service.Details
                                            </p>
                            }
                            else
                            {
                                            <p class="text-sm text-slate-500 italic">
                                                No description provided for this service.
                                            </p>
                            }
                                </div>

                                <!-- Card Footer -->
                                <div class="mt-auto border-t border-slate-100 bg-white px-6 py-4">
                            @if (User?.Identity?.IsAuthenticated ?? false)
                            {
                                @if (User.IsInRole("Member"))
                                {
                                                    <a asp-controller="AppointmentsMvc" asp-action="Book" asp-route-serviceId="@service.ServiceID"
                                                       class="inline-flex w-full items-center justify-center rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-emerald-500">
                                                        Book this service
                                                        <span class="ml-2" aria-hidden="true">→</span>
                                                    </a>
                                }
                                else
                                {
                                                    <div class="rounded-xl border border-slate-200 bg-slate-50 px-4 py-2.5 text-sm text-slate-600">
                                                        Booking is available for <span class="font-semibold text-slate-900">Members</span>.
                                                    </div>
                                }
                            }
                            else
                            {
                                            <a asp-area="Identity" asp-page="/Account/Login"
                                               class="inline-flex w-full items-center justify-center rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-900 shadow-sm hover:bg-slate-50">
                                                Log in to book
                                                <span class="ml-2" aria-hidden="true">→</span>
                                            </a>
                            }
                                </div>
                            </div>
                }
                </div>
        }
        else
        {
                <div class="rounded-2xl border border-dashed border-slate-200 bg-white p-10 text-center">
                    <p class="text-sm font-semibold text-slate-900">No services available</p>
                    <p class="mt-1 text-sm text-slate-600">Please check back later.</p>
                </div>
        }
    </div>
</div>
